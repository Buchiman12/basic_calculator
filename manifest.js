let manifestList = {
  containerId: 0,
  destination: "",
  weight: 0,
  unit: "",
  hazmat: true,
}

let normalizeUnits = (manifest) => {
  return manifest.containerId = 68, manifest.destination = "Salinas", manifest.weight = 45.45,
  manifest.unit = "kg";

}
let validateManifest = (manifest) => {

}

let processManifest = (manifest) => {

}
console.log(normalizeUnits(manifestList.containerId))